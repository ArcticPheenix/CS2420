#include <iostream>
#include <fstream>
#include <string>
#include "HashTable.h"

void parseElements(ifstream& input);
void parseFormulas(ifstream& input);
void computeAtomicSum(std::string formula);
int main()
{
	return 0;
}